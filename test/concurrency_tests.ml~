module To_test = struct

  let test_concurrency1 () =
    let fm = File_manager.make in
    ""

end

let test_write_and_read_last_byte () =
  Alcotest.(check string)
    "verify last byte write and read" "offset 4091 contains a"
    (To_test.write_and_read_last_byte ())

let all_tests () =
  [
    Alcotest.test_case "test concurrency" `Quick
      test_concurrency1;
  ]
